# Explainability Examples

This folder stores transparent Grad-CAM overlays exported by:

```bash
python ml/explainability/generate_examples.py --images path/to/sample1.jpg path/to/sample2.jpg
```

Default fallback creates `placeholder_gradcam.png` when no images are provided.
